#Thie is a project that add some additional functions to xv6-system.

1. Add a system calls that count the system call number current thread had used.
2. Change the Robin schedule to Lottery schedule
